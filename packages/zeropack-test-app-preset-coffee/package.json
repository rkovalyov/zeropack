{
  "dependencies": {
    "react": "16.0.0",
    "react-dom": "16.0.0"
  },
  "devDependencies": {
    "zeropack-core": "^0.0.15",
    "zeropack-preset-coffee": "^0.0.16",
    "zeropack-storybook-react": "^0.0.1"
  },
  "scripts": {
    "start": "zeropack serve",
    "storybook": "zeropack-storybook-react serve",
    "serve": "zeropack serve",
    "build": "zeropack build",
    "test": "true"
  },
  "description": "zeropack contrib package",
  "repository": "https://github.com/zeropack/zeropack",
  "license": "MIT",
  "name": "zeropack-test-app-preset-coffee",
  "version": "0.0.16"
}
